name: ${project.name}
version: ${project.version}
description: ${project.description}
author: ${project.organization.name}
website: ${project.organization.url}

main: ${mainClass}
depend: [Spout, Vault]
load: POSTWORLD

permissions:  
  moveall.move:
    description: Allows users to use moveall
    default: op